<%- include('partials/_header') %> <%- include('partials/_navbar') %>
<div class="container">
  <div class="row row-cols-4">
    <div
      class="card col- mx-3 mt-4 bg-light mb-3 text-center"
      style="min-width: 20rem;"
    >
      <img src="#" class="card-img-top" alt="#" />
      <div class="card-body">
        <h5 class="card-title"></h5>
        <h6 class="card-subtitle mb-2 text-muted"></h6>
        <p class="card-text">CREATE PASSWORD</p>
        <a
          href="/passwords/new"
          class="btn btn-primary"
          style="margin-top: 50px;"
          >CREATE</a
        >
      </div>
    </div>
    <% pwds.forEach((pwd) => { %>
    <div
      class="card col- mx-3 mt-4 bg-light mb-3 text-center"
      style="min-width: 20rem;"
    >
      <img src="#" class="card-img-top" alt="#" />
      <div class="card-body">
        <h5 class="card-title"><%= pwd.website_title%></h5>
        <h6 class="card-subtitle mb-2 text-muted">
          URL: <%= pwd.website_url%>
        </h6>
        <p class="card-text">Username: <%= pwd.website_username%></p>
        <p class="card-text">Password: <%= pwd.website_pwd%></p>
        <a
          href="passwords/<%=pwd.id%>"
          data-toggle="modal"
          data-target="#modal_<%=pwd.id%>"
          class="btn btn-primary"
          id="open-pwd"
          >GO</a
        >
      </div>
    </div>

    <div
      class="modal fade"
      id="modal_<%=pwd.id%>"
      tabindex="-1"
      aria-labelledby="Password"
      aria-hidden="true"
    >
      <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="title"><%=pwd.website_title%></h5>
          </div>
          <div class="modal-body" id="pwd-info">
            <img src="https://cdn4.iconfinder.com/data/icons/core-ui-outlined/32/outlined_placeholder-512.png" style="width: 100px; height: auto"/>
            <div><%= pwd.website_url%></div>
            <div><%=pwd.website_username%></div>
            <input disabled id="passwordBox" type="text" value="<%= pwd.website_pwd%>">
            <button id="change">Change Password</button>
            <button id= "copy">Copy Password</button>
          </div>
          <div class="modal-footer">
            <button
              type="button"
              class="btn btn-secondary"
              data-dismiss="modal"
            >
              Close
            </button>
            <button type="button" class="btn btn-primary">Save changes</button>
          </div>
        </div>
      </div>
    </div>
    <% }); %>
  </div>
</div>

<%-include('partials/_footer') %>
